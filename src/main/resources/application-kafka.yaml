fint:
  kafka:
    topic:
      org-id: ${fint.org-id}
      domain-context: kontroll
    application-id: ${fint.application-id}
spring:
  kafka:
    consumer:
      group-id: ${fint.application-id}

novari:
  kafka:
    default-replicas: 1
    topic:
      domain-context: kontroll
      org-id: fintlabs-no #Husk å overstyre
    application-id: ${fint.org-id}